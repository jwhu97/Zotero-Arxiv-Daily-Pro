# ==================== 必需配置 ====================
# Zotero 配置（必填）
ZOTERO_ID=12345678                    # 你的 Zotero User ID（数字），从 https://www.zotero.org/settings/security 获取
ZOTERO_KEY=AB5tZ877P2j7Sm2Mragq041H  # Zotero API Key，从 https://www.zotero.org/settings/security 获取

# arXiv 查询配置（必填）
ARXIV_QUERY=cs.AI+cs.CV+cs.LG+cs.CL   # arXiv 分类，用 + 连接多个类别

# 邮件配置（必填）
SMTP_SERVER=smtp.qq.com               # SMTP 服务器地址
SMTP_PORT=465                         # SMTP 端口（465 用于 SSL，587 用于 TLS）
SENDER=your_email@qq.com              # 发件邮箱
SENDER_PASSWORD=your_smtp_password    # SMTP 授权码（不是邮箱登录密码！）
RECEIVER=receiver@outlook.com         # 收件邮箱


# ==================== 可选配置 ====================
# 论文数量限制
MAX_PAPER_NUM=50                      # 最多推荐的论文数，-1 表示不限制（默认 100）

# 空邮件发送
SEND_EMPTY=False                      # 没有新论文时是否发送空邮件（默认 False）

# Zotero 过滤规则（gitignore 风格）
ZOTERO_IGNORE=                        # 例如：AI Agent/\n**/survey\n!LLM/survey

# LLM 配置（默认使用本地 Qwen2.5-3B 模型）
USE_LLM_API=0                         # 是否使用 API（0=本地模型，1=API）
LANGUAGE=Chinese                      # TLDR 语言（默认 English）

# 如果 USE_LLM_API=1，则需要以下配置：
OPENAI_API_KEY=sk-xxx                 # API Key（推荐使用 SiliconFlow 免费额度）
OPENAI_API_BASE=https://api.siliconflow.cn/v1  # API 地址
MODEL_NAME=Qwen/Qwen2.5-7B-Instruct   # 模型名称
